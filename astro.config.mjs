// @ts-check
import { defineConfig } from 'astro/config';

// Static single-page site. Default output ('static') is what we want.
export default defineConfig({
  site: 'https://angelobaleno.com',
});
